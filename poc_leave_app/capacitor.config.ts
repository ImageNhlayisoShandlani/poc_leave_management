import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'poc_leave_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
